---
import "../style.css";
---

<div id="mc_embed_shell" class="hidden">
  <div id="mc_embed_signup">
    <div class="close">
      <p>&#215;</p>
    </div>
    <form action="https://bobles.us22.list-manage.com/subscribe/post?u=89bd6125853d0844ca6f04323&id=84b9bec865&f_id=000ac8e1f0" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank">
      <div id="mc_embed_signup_scroll">
        <h2>Tilmeld dig vores nyhedsbrev og få 10% rabat!</h2>
        <div class="indicates-required"><span class="asterisk">*</span> indicates required</div>
        <div class="mc-field-group">
          <label for="mce-EMAIL">Email Adresse <span class="asterisk">*</span></label>
          <input type="email" name="EMAIL" class="required email" id="mce-EMAIL" required="" value="" />
        </div>
        <div class="mc-field-group">
          <label for="mce-FNAME">Fornavn <span class="asterisk">*</span></label>
          <input type="text" name="FNAME" class="required text" id="mce-FNAME" value="" required="" />
        </div>
        <div id="mce-responses" class="clear foot">
          <div class="response" id="mce-error-response" style="display: none;"></div>
          <div class="response" id="mce-success-response" style="display: none;"></div>
        </div>
        <div aria-hidden="true" style="position: absolute; left: -5000px;">
          <input type="text" name="b_89bd6125853d0844ca6f04323_84b9bec865" tabindex="-1" value="" />
        </div>
        <div class="optionalParent">
          <div class="clear foot">
            <input type="submit" name="subscribe" id="mc-embedded-subscribe" class="mcbutton" value="Tilmeld dig" />
          </div>
        </div>
      </div>
      <p class="p-small">
        Ved at tilmelde dig giver du samtykke til at modtage e-mails fra bObles om Tumletid-begivenheder og andre relaterede opdateringer. Vi respekterer dit privatliv og vil kun bruge din e-mailadresse til at sende dig relevant information. Du kan
        til enhver tid afmelde dig.
      </p>
    </form>
  </div>
</div>

<style type="text/css">
  #mc_embed_signup {
    background: #fff;
    clear: left;
    font:
      14px Helvetica,
      Arial,
      sans-serif;
    position: relative;
    width: 20rem;
  }

  #mc_embed_shell {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    box-shadow: rgba(0, 0, 0, 0.16) 0px 1px 4px;
    z-index: 100;
    background-color: var(--p-white);
    padding: var(--xs-spacing);
  }

  h2 {
    text-transform: uppercase;
    font-size: var(--h4-font-size) !important;
    width: 28ch;
  }

  .mcbutton {
    text-transform: uppercase;
    background-color: transparent;
    height: fit-content;
    width: fit-content;
    padding: var(--xs-spacing) var(--s-spacing);
    font-family: var(--body-font);
    font-size: var(--p-font-size);
    border-radius: 40px !important;
    border: 1px solid var(--p-blue) !important;
    color: var(--p-white);
    background-color: var(--p-blue);
    cursor: pointer;
    letter-spacing: 0.075rem;
    transition: 150ms;
    margin: 0 !important;
  }

  label {
    text-transform: uppercase;
    letter-spacing: 0.75px;
  }

  #mc_embed_signup .foot {
    display: grid;
    grid-template-columns: 1fr;
    width: 96%;
    align-items: center;
    margin-block: var(--m-spacing);
  }

  .close {
    position: absolute;
    top: 0%;
    right: 5%;
    cursor: pointer;
    z-index: 120;
    & p {
      font-size: var(--h3-font-size);
    }
  }

  .closed {
    display: none;
  }

  @media (min-width: 800px) {
    #mc_embed_signup {
      background: #fff;
      clear: left;
      font:
        14px Helvetica,
        Arial,
        sans-serif;
      position: relative;
      width: 40rem;
    }

    #mc_embed_shell {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      box-shadow: rgba(0, 0, 0, 0.5) 0px 1px 4px;
      z-index: 100;
      background-color: var(--p-white);
      padding: var(--m-spacing);
    }

    .close {
      & p {
        font-size: var(--h2-font-size);
      }
    }
    h2 {
      text-transform: uppercase;
      font-size: var(--h2-font-size);
      width: 28ch;
    }
  }
</style>

<script type="text/javascript" src="//s3.amazonaws.com/downloads.mailchimp.com/js/mc-validate.js"></script>
<script type="text/javascript">
  (function ($) {
    window.fnames = new Array();
    window.ftypes = new Array();
    fnames[0] = "EMAIL";
    ftypes[0] = "email";
    fnames[1] = "FNAME";
    ftypes[1] = "text";
    fnames[2] = "LNAME";
    ftypes[2] = "text";
    fnames[3] = "ADDRESS";
    ftypes[3] = "address";
    fnames[4] = "PHONE";
    ftypes[4] = "phone";
    fnames[5] = "BIRTHDAY";
    ftypes[5] = "birthday";
  })(jQuery);
  var $mcj = jQuery.noConflict(true);
</script>
<script>
  document.addEventListener("scroll", function () {
    var scrolled = window.scrollY;

    if (scrolled >= 1500) {
      document.querySelector("#mc_embed_shell").classList.remove("hidden");
    }
  });

  const close = document.querySelector(".close");
  const subscribed = document.querySelector("#mc-embedded-subscribe");

  const hideSignupForm = () => {
    document.getElementById("mc_embed_shell").classList.add("closed");
  };

  close.addEventListener("click", hideSignupForm);
  subscribed.addEventListener("click", hideSignupForm);
</script>
